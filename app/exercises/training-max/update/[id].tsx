/**
 * Elite Locker - Update Training Max Screen
 * Allows users to update their existing training max for an exercise
 * This is essentially the same as the set screen but with different context
 */

export { default } from '../set/[id]';
